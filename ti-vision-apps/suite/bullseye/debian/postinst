#!/bin/sh
#
# see: dh_installdeb(1)

set -e

if [ -f /usr/lib/libvx_tidl_rt.so.map ] ; then
	rm -f /usr/lib/libvx_tidl_rt.so.map || true
fi

if [ -f /usr/lib/libtidl_onnxrt_EP.so.map ] ; then
	rm -f /usr/lib/libtidl_onnxrt_EP.so.map || true
fi

if [ -f /usr/lib/libtidl_tfl_delegate.so.map ] ; then
	rm -f /usr/lib/libtidl_tfl_delegate.so.map || true
fi

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
