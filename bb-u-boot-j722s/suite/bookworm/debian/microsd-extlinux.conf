menu title j722s microSD (extlinux.conf)

timeout 50

default microSD (default)

label microSD Recovery
    kernel /Image
    append console=ttyS2,115200n8 root=/dev/mmcblk0p2 ro rootfstype=ext4 rootwait net.ifnames=0
    fdtdir /
    fdt /ti/k3-j722s-evm.dtb
    #fdtoverlays /overlays/<file>.dtbo
    initrd /initrd.img

label microSD (RPI 7inch panel)
    kernel /Image
    append console=ttyS2,115200n8 root=/dev/mmcblk0p2 ro rootfstype=ext4 rootwait net.ifnames=0
    fdtdir /
    fdt /ti/k3-j722s-evm.dtb
    fdtoverlays /overlays/k3-j722s-evm-dsi-rpi-7inch-panel.dtbo
    initrd /initrd.img

label microSD (lincolntech-185lcd panel)
    kernel /Image
    append console=ttyS2,115200n8 root=/dev/mmcblk0p2 ro rootfstype=ext4 rootwait net.ifnames=0
    fdtdir /
    fdt /ti/k3-j722s-evm.dtb
    fdtoverlays /overlays/k3-j722s-evm-lincolntech-185lcd-panel.dtso
    initrd /initrd.img

label microSD (csi0 imx219)
    kernel /Image
    append console=ttyS2,115200n8 root=/dev/mmcblk0p2 ro rootfstype=ext4 rootwait net.ifnames=0
    fdtdir /
    fdt /ti/k3-j722s-evm.dtb
    fdtoverlays /overlays/k3-j722s-evm-csi0-imx219.dtbo
    initrd /initrd.img

label microSD (csi1 imx219)
    kernel /Image
    append console=ttyS2,115200n8 root=/dev/mmcblk0p2 ro rootfstype=ext4 rootwait net.ifnames=0
    fdtdir /
    fdt /ti/k3-j722s-evm.dtb
    fdtoverlays /overlays/k3-j722s-evm-csi1-imx219.dtbo
    initrd /initrd.img

label microSD (default)
    kernel /Image
    append console=ttyS2,115200n8 root=/dev/mmcblk0p2 ro rootfstype=ext4 rootwait net.ifnames=0
    fdtdir /
    fdt /ti/k3-j722s-evm.dtb
    #fdtoverlays /overlays/<file>.dtbo
    initrd /initrd.img
